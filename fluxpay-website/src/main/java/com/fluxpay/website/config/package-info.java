/**
 * 官网模块基础设施配置。
 *
 * <p>包含：
 * <ul>
 *   <li>{@code WebMvcConfig} — 跨域（CORS）配置，允许前端域名访问</li>
 *   <li>{@code MailConfig} — JavaMailSender 参数绑定，用于发送验证码与通知邮件</li>
 * </ul>
 */
package com.fluxpay.website.config;
