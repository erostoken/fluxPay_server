/**
 * MapStruct 对象映射接口。
 *
 * <p>定义官网模块 Entity ↔ DTO / VO 的转换接口，由 MapStruct 在编译期自动生成实现类。
 * 使用 {@code @Mapper(componentModel = "spring")} 注解，以便通过 Spring 注入。
 */
package com.fluxpay.website.mapstruct;
