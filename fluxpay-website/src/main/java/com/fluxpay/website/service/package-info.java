/**
 * 官网业务 Service。
 *
 * <p>包含：
 * <ul>
 *   <li>{@code CaptchaService} — 图形验证码的生成与 Redis 缓存校验</li>
 *   <li>{@code MailService} — 封装验证码邮件、密码重置邮件的发送逻辑</li>
 *   <li>{@code RegisterService} — 企业注册流程：参数校验、企业创建、发送激活邮件</li>
 * </ul>
 */
package com.fluxpay.website.service;
